#Write a R program to create a list of elements using vectors,
#matrices and a functions. Print the content of the list.

li =list(
  c(1,4,5,3,3,4,2,3,5,4,33),
  month.abb,
  matrix(1:4, nrow = 2),
  asin,
  data.frame(
    name=c("hshs")
  )
)


print(li)
